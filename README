A Go library for iteration and manipulation of collection types.

Where possible known interfaces are used to reduce processing overhead, but the fallback case of type-generic collection
manipulation is also handled where it seems possible.